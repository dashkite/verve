stylus:
  targets:

    browser:
      - preset: css
        glob:
          - src/**/*.styl

      - preset: js
        glob:
          - src/**/*.styl

    node:
      - preset: css
        glob:
          - src/**/*.styl

      - preset: js
        glob:
          - src/**/*.styl
